{
  "buildCommand": "cd ../.. && pnpm install && pnpm exec prisma generate --schema=./prisma/schema.prisma && cd src/web && pnpm install && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "nextjs"
}
