{
  "buildCommand": "cd src/web && pnpm install && pnpm build",
  "outputDirectory": "src/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}

