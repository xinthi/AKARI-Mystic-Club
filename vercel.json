{
  "buildCommand": "cd ../.. && pnpm install && pnpm prisma:generate && cd src/web && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "nextjs",
  "routes": [
    {
      "src": "/api/webhook",
      "dest": "/api/webhook.js"
    }
  ],
  "functions": {
    "api/webhook.js": {
      "runtime": "nodejs18.x",
      "maxDuration": 30
    }
  }
}
